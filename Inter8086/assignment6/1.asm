PORT_0 EQU 3040H 
PORT_C EQU 3043H
STACK1 SEGMENT
	DW 256 DUP(0)
STACK1 ENDS

CODE SEGMENT 
	ASSUME CS:CODE
START:	MOV DX, PORT_C
		MOV AL, 00110100B
		OUT DX, AL
		MOV DX, PORT_0
		MOV AX, 9216H
		OUT DX, AL
		MOV AL, AH
		OUT DX, AL
WAIT1:	MOV AH, 1
		INT 16H
		JZ WAIT1
		MOV AX, 4C00H
		INT 21H
CODE ENDS
		END START